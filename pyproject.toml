[project]
name = "breakglass-dynamic-secrets"
version = "0.1.0"
description = "An event-driven python syslog parser for root-password rotation on Juniper enterprise network equipment"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

# [build-system]
# requires = ["uv_build >= 0.11.32, <0.12.0"]
# build-backend = "uv_build"
